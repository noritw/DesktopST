<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
      android:usesCleartextTraffic 一定要留著：本機模型（Ollama／LM Studio）走區網
      http://，targetSdk 28 起系統預設禁止明文流量，原生 CapacitorHttp 的請求會被
      直接擋掉（WebView 的 allowMixedContent 蓋不到原生層）。DeST 主 App 一直有
      這一行，這個 App 建立時漏了。
    -->
    <application
        android:allowBackup="true"
        android:usesCleartextTraffic="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!--
              桌面小工具（相機／鉛筆／其餘區域）點擊深連結，@capacitor/app 的
              appUrlOpen／getLaunchUrl() 收下，見 widget/NutritionWidgetProvider.kt
              與 docs/nutrition-widget-plan.md §5。
            -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="@string/custom_url_scheme" />
            </intent-filter>

            <!--
              DeST 手機版「匯出設定給食記」走 Android 分享面板送過來（同一支手機，
              不用開相機掃 QR），見 share/ShareReceiverPlugin.kt。這會讓食記在任何
              App 分享純文字時都出現在分享面板選單裡——是預期行為，收到後 JS 端
              會驗證格式，不是 DeST 匯出的內容就顯示「看不懂這段內容」。
            -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>

        </activity>

        <receiver
            android:name=".widget.NutritionWidgetProvider"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <intent-filter>
                <action android:name="tw.nori.destnutrition.widget.ACTION_NUTRITION_WIDGET_REFRESH" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_nutrition_info" />
        </receiver>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
</manifest>
