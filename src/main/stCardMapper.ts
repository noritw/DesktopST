/**
 * 轉出檔：本體已搬到 src/core/card/stCardMapper.ts（B1 抽 core）。
 * 保留此路徑讓既有 import 不用改；新增檔案請直接 import core/。
 */
export { mergeStPersonality, importStJson, exportToStJson } from '../core/card/stCardMapper'
