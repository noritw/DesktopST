/**
 * 連結閱讀的模組 id。
 *
 * 開關本身走既有的「模組開關」清單（桌面 `mobileModuleToggleDefs`／
 * 手機 `MODULE_DEFS`／情境覆蓋 `SCENE_MODULE_ROWS_BASE`），
 * 所以 S2 M5 設定同步、情境覆蓋都不必另外接線。
 */
export const LINK_READER_MODULE_ID = 'desktopst.link-reader'
