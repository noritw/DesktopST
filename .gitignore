node_modules/
out/
dist/
.DS_Store
*.local
*.log
.env
.env.*
!.env.example
.claude/settings.json
.claude/settings.local.json
.claude/scheduled_tasks.lock
.claude/worktrees/
.kiro/
.vs/
test-extract/
scripts/generateNotificationSound.js
generate-sounds.js
新使用者啟動流程改進計劃.md
DesktopST-portable.zip
DesktopST-v*.zip
scripts/sync-and-push-intro.bat
assets/*.dstpack

# 全等比對 harness 的暫存輸出（golden 快照另存於 scripts/__golden__/）
.eq*.cjs
.eq*.json
.sh*.cjs
.sh*.json

# Capacitor 產生的原生樹（`npx cap add android` 可重建，所以整包不進 repo）
#
# ⚠️ **例外：手寫的 Java 與 AndroidManifest 一定要進版控。**
# 2026-08-11 起 android/app/src/main/java/tw/nori/dest/ 底下有提醒鬧鐘的原生實作
# （AlarmManager、開機重註冊、通知），那是真的原始碼，不是產生物——
# 整包忽略的話重建專案就沒了。
#
# git 不會走進被忽略的目錄，所以必須逐層 un-ignore 才能讓底下的例外生效。
/android/*
!/android/app
/android/app/*
!/android/app/src
/android/app/src/*
!/android/app/src/main
/android/app/src/main/*
!/android/app/src/main/java
!/android/app/src/main/AndroidManifest.xml
/out/mobile/

# 測試用 QR 圖（MobileST-*-test.bat 產生）
mobile-test-qr.png

# 圖示預覽圖（scripts/gen-android-icons.ps1 -Preview 產生，看完即可刪）
icon-preview.png

# Android 正式簽章 keystore／密碼。`android/keystore.properties` 本來就落在
# 上面 `/android/*` 的忽略範圍內，這裡是防呆——不管檔案放在哪一層，
# 只要副檔名或檔名對到就一律不進版控。弄丟 keystore＝所有使用者都無法升級，
# 但「不小心 commit 進去」風險更高更常見，見 docs/pre-b3-work-assessment.md §9。
*.jks
*.keystore
keystore.properties
